import 'package:flutter/material.dart';

class CursesPage extends StatelessWidget {
  const CursesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(), body: Container());
  }
}
